<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}URL Shortener{% endblock %}</title>
</head>
<body>

<div class="navbar">
    <a href="{{ path('home') }}">Главная</a>
    <a href="{{ path('all_links') }}">Мои ссылки</a>
</div>

<div class="container">
    {% block content %}{% endblock %}
</div>

</body>
</html>
